interface:
  display_name: "Foremerge"
  short_description: "Coordinate coding agents safely above Git"
  default_prompt: "Use $foremerge to publish intent, coordinate semantic conflicts, verify the ChangeSet, and record the integrated work."
